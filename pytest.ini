[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -ra
markers =
    unit: unit tests exercising a single function in isolation
    integration: integration tests wiring modules together with mocked I/O
    e2e: end-to-end tests driving the bot through Telegram update dispatch
